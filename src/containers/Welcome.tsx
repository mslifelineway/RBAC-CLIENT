import React from "react";
import { PrivateLayout } from "../components";

export const Welcome = () => {
  return (
    <PrivateLayout>Welcome to RBAC (Role based access control)</PrivateLayout>
  );
};
